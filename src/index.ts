import { getWindows, openApp, openCameraStreamWindow, playAudio } from "../sdk/main";
import * as fs from "fs";

async function main() {
    // await openApp();
    // await openCameraStreamWindow();
    setTimeout(async () => {
    }, 5000);
}
main();


